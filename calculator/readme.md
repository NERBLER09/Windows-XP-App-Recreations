## Recreation Of The Windows XP - Calculator
This is where the recreation of the Windows XP Calculator is.

## Running 
Run `npm i` to install the dependencies. Run `npm start` to start the electron window.